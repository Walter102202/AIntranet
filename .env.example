# Configuración del Portal de Intranet
# Copia este archivo a .env y completa con tus valores

# ==================================================
# BASE DE DATOS MYSQL (Requerido)
# ==================================================
DB_HOST=localhost
DB_USER=tu_usuario
DB_PASSWORD=tu_password
DB_NAME=intranet_db

# ==================================================
# USUARIO ADMINISTRADOR INICIAL
# ==================================================
ADMIN_USERNAME=admin
ADMIN_PASSWORD=tu_password_seguro
ADMIN_EMAIL=admin@tuempresa.com

# ==================================================
# API DEL LLM (Requerido para el Chatbot)
# ==================================================
# Obtén tu API key en: https://platform.openai.com/api-keys
LLM_API_KEY=tu_api_key_aqui


# ==================================================
# CONFIGURACIÓN OPCIONAL DEL LLM
# ==================================================

# URL base de la API (para usar otros proveedores)
# OpenAI: https://api.openai.com/v1
# Azure OpenAI: https://tu-recurso.openai.azure.com/openai/deployments/tu-deployment
# Ollama: http://localhost:11434/v1
LLM_API_BASE=https://api.openai.com/v1

# Modelo a utilizar
# RECOMENDADO PARA VISIÓN:
# - gpt-5.1 (reasoning model con visión - MÁS AVANZADO)
# - gpt-4o (optimizado para visión - MÁS ECONÓMICO)
# - gpt-4-turbo (potente pero más costoso)
# Otros:
# - gpt-4o-mini (económico, sin reasoning)
# - gpt-3.5-turbo (legacy)
LLM_MODEL=gpt-5.1

# Máximo de tokens por respuesta (2000-8000 recomendado para visión)
LLM_MAX_TOKENS=4000

# Temperatura (creatividad del modelo: 0.0-1.0)
# 0.0 = Más determinista y enfocado
# 1.0 = Más creativo y variado
# Recomendado: 0.3-0.5 para análisis de datos
LLM_TEMPERATURE=0.3

# ==================================================
# CONFIGURACIÓN PARA GPT-5.1 (REASONING MODELS)
# ==================================================

# Verbosity (solo para GPT-5.1)
# Controla el nivel de detalle en el razonamiento
# Valores: low, medium, high
LLM_VERBOSITY=medium

# Reasoning effort (solo para GPT-5.1)
# Controla el nivel de razonamiento del modelo
# Valores: none, low, medium, high
# IMPORTANTE: Para conversación general usar 'medium'
LLM_REASONING_EFFORT=medium

# Reasoning effort para VISIÓN (solo para GPT-5.1)
# CRÍTICO: Para análisis de imágenes/gráficos usar 'low' o 'none'
# Investigación muestra que high reasoning EMPEORA el rendimiento en visión
# Valores: none, low, medium, high
# RECOMENDADO: low
LLM_VISION_REASONING_EFFORT=low

# ==================================================
# OAUTH - MICROSOFT (AZURE AD) - Opcional
# ==================================================
# Para habilitar login con Microsoft, configura estos valores
# Ver OAUTH_SETUP.md para instrucciones detalladas
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
MICROSOFT_TENANT_ID=common
MICROSOFT_REDIRECT_URI=http://localhost:5000/auth/microsoft/callback

# ==================================================
# OAUTH - GOOGLE - Opcional
# ==================================================
# Para habilitar login con Google, configura estos valores
# Ver OAUTH_SETUP.md para instrucciones detalladas
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:5000/auth/google/callback

# ==================================================
# NOTAS
# ==================================================
# - Si no configuras LLM_API_KEY, el chatbot funcionará en modo de desarrollo
#   con respuestas simuladas (útil para testing)
# - Para instalar el soporte de .env, ejecuta: pip install python-dotenv
# - Agrega esto al inicio de app.py:
#   from dotenv import load_dotenv
#   load_dotenv()
# - OAuth es opcional: si no configuras las credenciales, solo estará disponible
#   el login tradicional con usuario/contraseña
